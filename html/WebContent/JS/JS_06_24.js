/**
 * 객체 
 */

//window.alert("즐")

// //객체 선언.
// let produck={
//     //속성
//     제품명:"아이스 아메리카노",
//     유형: "커피",
//     //메소드(함수)
//     eat : function(drink){
//         alert(drink+"를 마십니다.")
//     }

// }

// alert(produck.제품명)
// produck.eat("라떼")

// -----------------------------------------------------------------
/*
// javascript 선택자 

 window.onload=function(){ 
    
    var header_id=document.getElementById("header1");             // id를 이용한 선택자
    var header_class=document.getElementsByClassName("header3");  // class를 이용한 선택자   
    var header_tag=document.getElementsByTagName("h1");          // tag를 이용한 선택자
        console.log(header_id);
        console.log(header_class);
        console.log(header_tag);
 

    // document.querySelector

    
    let header_puID=document.querySelector("#header1");         //단수
    let header_puClass=document.querySelectorAll(".header3");   //복수  --All
    let header_puTAG=document.querySelectorAll("h1");           //복수  --All
        console.log(header_puID);
        console.log(header_puClass);
        console.log(header_puTAG);

    var id=document.querySelector("p")
    //열고 닫고가 가능한 태그에만 쓸수 있음.
    // p태그를 innerHTML영역에 문자열을 넣어라. 
    id.innerHTML="5~20자의 영문 소문자, 숫자와 특수기호(_),(-)만 사용 가능합니다.";
    // p태그의 글자색(color)를 빨간색으로 적용
    id.style.color="red";
    // p태그의 배경색(background-color)을 노란색으로 적용
    id.style.backgroundColor="yellow";  // '-'대신 대문자 사용

    var img=document.querySelector("img");
    
    var button=document.querySelector("input");
    // 사진변경 버튼을 클릭하면
    button.onclick=function(){
        img.setAttribute("src","../JS_HTML/고양이.jpg");
    }
    alert(img.getAttribute("src"));
    img.setAttribute("src","../JS_HTML/다운로드.jpg");
    alert(img.getAttribute("src"));


}
*/
//--------------------------------------------------------------------------------------------
